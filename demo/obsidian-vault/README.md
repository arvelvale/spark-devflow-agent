# 演示纪要库

DGX-Spark 开发流 agent 的演示用 Obsidian 文件夹。内容全部虚构，围绕演示项目 `tinyledger`（小型 Python 记账 CLI）。

- agent 的 `meeting-to-tasks` 等技能从 `会议纪要/` 读取纪要
- 这些文本会进入 JEV / StepFun 的请求，**不要放真实笔记**
- 在 Obsidian 里的入口：`Aerchen/项目/DGX-Spark-演示纪要`（junction 指向本目录）
