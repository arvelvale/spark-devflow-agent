# DGX Spark 协作入口

先读 `README.md`（代码结构、命令、分工入口），再读 `docs/README.md`（设计文档索引）。
连接和模型运维以 `docs/节点连接与模型运维.md` 为正本。

## 硬约定

1. 凭据（节点账号、JEV / StepFun / Linear key）只在本地登录表和 `.env` 里，不写进文档、代码、日志、轨迹。节点 IP 和用户名也不入库。
2. SSH 端口只用 6006；遵守节点规则：不改系统账户、SSH、网络、防火墙、驱动，不重启，长任务用 tmux，磁盘留 20%。
3. 阈值是设计值：改之前读 `agent/config.py` 里的方向注释，改完跑对应评估（`eval/`），把数据写进架构文档的"实现状态"。
4. 三个接口（`docs/接口/`）是团队契约：只增不改，要改先和负责人确认。
5. Linear 只动演示项目「DGX-Spark Agent 演示」；`--yes` 只在演示沙盒里用。
6. 宣布一项功能完成前，要在节点上用真实模型跑过，并附轨迹或数据。
